# Autogenerated from root-to-user role on {{ template_run_date }}
# !!! You should strongly consider encrypting this file !!!
#  ansible-vault encrypt {{ template_destpath }}

all:
  hosts:
    {{ ansible_hostname }}:
      ansible_host: "{{ inventory_hostname | split('@') | last }}"
      ansible_user: "{{ username }}"
      ansible_ssh_pass: "{{ password }}"
      ansible_become_pass: "{{ password }}"
      ansible_connection: ssh

